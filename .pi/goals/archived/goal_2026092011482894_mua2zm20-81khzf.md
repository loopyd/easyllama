{
  "version": 3,
  "id": "mua2zm20-81khzf",
  "objective": "Return the EasyLlama Qwen stack to the documented llama-swap-owned swap design — reverting my co-residency change and the publish/warm-service bypass — and prove the proxy reliably serves the chat and auxiliary routes so Hindsight's queue resumes draining. Steady-state policy: the aux models (embeddings, reranker) may stay warm alongside the chat, but only when vLLM's startup gate is still satisfied, with no external `/run` holder competing with the proxy.",
  "status": "paused",
  "autoContinue": false,
  "usage": {
    "tokensUsed": 820120,
    "activeSeconds": 4777
  },
  "sisyphus": false,
  "createdAt": "2026-09-20T17:19:22.728Z",
  "updatedAt": "2026-09-20T18:49:15.473Z",
  "revision": 41,
  "scheduler": {
    "version": 1,
    "owner": "01a0bac0-9b7a-70e5-94fe-a1c54e7f3965",
    "generation": "690e14e9-a4ae-48bf-b118-9e6a9cf1dfe3",
    "used": 0,
    "phase": "ready",
    "repairUsed": false,
    "decision": {
      "kind": "ready",
      "nextAction": "Continue the goal at the user's request.",
      "purpose": "kickoff"
    }
  },
  "taskList": {
    "tasks": [
      {
        "id": "task-1",
        "title": "Revert the co-residency change and the bypass in both repos, with the live system matching",
        "status": "complete",
        "verificationContract": "llamacpp: revert the co-residency commit and the publish-feature commit (revert commits only, no history rewrite); config/config.qwen.yml.example back to --gpu-memory-utilization 0.90, --max-num-seqs 16, --max-num-batched-tokens 8192 and NO publish key; ruff clean + pytest 0 failures. dotfiles: revert the direct-wiring and warm-service commits; no easyllama-warm unit/helper in the manifest or tree. Live config/config.qwen.yml mirrors the restored .example. Both repos committed, pushed, and `git status` clean with local HEAD == origin/main.",
        "completedAt": "2026-09-20T17:20:25.587Z",
        "evidence": "llamacpp: revert commits e3843b1 (publish feature) + 7bcdc86 (co-residency); .example back to --gpu-memory-utilization 0.90 / --max-num-seqs 16 / --max-num-batched-tokens 8192 with no publish key; pub"
      },
      {
        "id": "task-2",
        "title": "Reap the stranded GPU memory and restore a clean stack baseline",
        "status": "complete",
        "verificationContract": "Restart the easyllama stack so the crash-looping chat container is recreated. Evidence: all 5 containers healthy; the chat engine starts with no 'Free memory on device cuda:0 ... less than desired GPU memory utilization' error; nvidia-smi memory.used is fully accounted for by the visible process table (used minus attributed approximately 0, i.e. no stranded allocation); no vLLM crash-loop in the logs over a 2-minute window.",
        "completedAt": "2026-09-20T17:26:08.247Z",
        "evidence": "Removed the installed warm artifacts (easyllama-warm.service unit + wants symlink + ~/.local/lib/dotfiles/easyllama-warm.py; is-enabled=not-found) and recreated the stack. Evidence: all 5 easyllama co"
      },
      {
        "id": "task-3",
        "title": "Point Hindsight back at the swap proxy for embeddings and rerank",
        "status": "complete",
        "verificationContract": "server.env.tmpl embeddings base URL back to the proxy and rerank back to the proxy; renderer run with manifest hash refresh; Hindsight container recreated and `hindsight.py health` reports ready; the app log confirms Embeddings/Reranker providers initialising against the proxy endpoint.",
        "completedAt": "2026-09-20T17:27:25.021Z",
        "evidence": "server.env.tmpl: HINDSIGHT_API_EMBEDDINGS_OPENAI_BASE_URL=http://127.0.0.1:8080/v1 and HINDSIGHT_API_EMBEDDINGS_OPENAI_MODEL=qwen3-embeddings (was 20128/v1 + qwen3-chat/qwen3-embeddings), embeddings A"
      },
      {
        "id": "task-4",
        "title": "Make the proxy own the aux lifecycle with co-residency within headroom",
        "status": "pending",
        "verificationContract": "Configure the proxy so the chat can always start (its vLLM startup gate satisfied) while the aux models are allowed to load into the leftover headroom afterwards — i.e. the aux group is evictable by the chat's swap rather than un-swappable; no publish flags and no warm service anywhere. Evidence: cold start of the chat succeeds with the aux models asleep, then the aux routes load successfully while the chat stays resident, and a second chat restart still succeeds."
      },
      {
        "id": "task-5",
        "title": "End-to-end verification window",
        "status": "pending",
        "verificationContract": "Through 127.0.0.1:8080: /v1/chat/completions, /v1/embeddings and /v1/rerank each return 200 (with expected shapes: embedding dim 1024, rerank results present). Zero 'upstream command exited prematurely' and zero 500s for those routes in a fresh log window. Hindsight: health ready, and `pending` declines measurably over a >=10-minute window with 0 new embedding/rerank errors and 0 new openai_responses_llm retry storms. Report the measured pending delta."
      },
      {
        "id": "task-6",
        "title": "Make a stalled or failed model start observable",
        "status": "pending",
        "verificationContract": "A start that cannot complete must stop being a silent multi-minute hang: bound the route health-check wait to a sane value and/or surface a loading state, so the failure appears as an error (and in the log) rather than an indefinite silence. Evidence: a documented before/after of the client-visible behaviour for a deliberately failing route, and the chosen config value recorded in the dotfiles."
      }
    ],
    "blockCompletion": true,
    "proposedAt": "2026-09-20T17:18:55.400Z"
  },
  "currentTaskId": "task-4",
  "stopReason": "user",
  "archivedPath": ".pi/goals/archived/goal_2026092011482894_mua2zm20-81khzf.md"
}

# Goal Prompt

Return the EasyLlama Qwen stack to the documented llama-swap-owned swap design — reverting my co-residency change and the publish/warm-service bypass — and prove the proxy reliably serves the chat and auxiliary routes so Hindsight's queue resumes draining. Steady-state policy: the aux models (embeddings, reranker) may stay warm alongside the chat, but only when vLLM's startup gate is still satisfied, with no external `/run` holder competing with the proxy.

## Progress

- Status: paused
- Auto-continue: off
- Sisyphus mode: no
- Time spent: 1h19m37s
- Tokens used: 820K (820,120) tokens
## Tasks

<!-- blockCompletion: true -->
- [x] task-1: Revert the co-residency change and the bypass in both repos, with the live system matching — evidence: llamacpp: revert commits e3843b1 (publish feature) + 7bcdc86 (co-residency); .example back to --gpu-memory-utilization 0.90 / --max-num-seqs 16 / --max-num-batched-tokens 8192 with no publish key; pub
- [x] task-2: Reap the stranded GPU memory and restore a clean stack baseline — evidence: Removed the installed warm artifacts (easyllama-warm.service unit + wants symlink + ~/.local/lib/dotfiles/easyllama-warm.py; is-enabled=not-found) and recreated the stack. Evidence: all 5 easyllama co
- [x] task-3: Point Hindsight back at the swap proxy for embeddings and rerank — evidence: server.env.tmpl: HINDSIGHT_API_EMBEDDINGS_OPENAI_BASE_URL=http://127.0.0.1:8080/v1 and HINDSIGHT_API_EMBEDDINGS_OPENAI_MODEL=qwen3-embeddings (was 20128/v1 + qwen3-chat/qwen3-embeddings), embeddings A
- [ ] task-4: Make the proxy own the aux lifecycle with co-residency within headroom — contract: Configure the proxy so the chat can always start (its vLLM startup gate satisfied) while the aux models are allowed to load into the leftover headroom afterwards — i.e. the aux group is evictable by the chat's swap rather than un-swappable; no publish flags and no warm service anywhere. Evidence: cold start of the chat succeeds with the aux models asleep, then the aux routes load successfully while the chat stays resident, and a second chat restart still succeeds.
- [ ] task-5: End-to-end verification window — contract: Through 127.0.0.1:8080: /v1/chat/completions, /v1/embeddings and /v1/rerank each return 200 (with expected shapes: embedding dim 1024, rerank results present). Zero 'upstream command exited prematurely' and zero 500s for those routes in a fresh log window. Hindsight: health ready, and `pending` declines measurably over a >=10-minute window with 0 new embedding/rerank errors and 0 new openai_responses_llm retry storms. Report the measured pending delta.
- [ ] task-6: Make a stalled or failed model start observable — contract: A start that cannot complete must stop being a silent multi-minute hang: bound the route health-check wait to a sane value and/or surface a loading state, so the failure appears as an error (and in the log) rather than an indefinite silence. Evidence: a documented before/after of the client-visible behaviour for a deliberately failing route, and the chosen config value recorded in the dotfiles.

